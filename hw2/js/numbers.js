document.addEventListener('DOMContentLoaded', () => {
    const fiveDigitNumber = prompt('Enter a five-digit number','');

    alert(`${fiveDigitNumber[0]} ${fiveDigitNumber[1]} ${fiveDigitNumber[2] } ${fiveDigitNumber[3] } ${fiveDigitNumber[4] }`);
});